registry:
  helm: nebula
  docker: scalecube

namespaces:
  protected:
    - master
    - develop

  prerelease:
    namespace: rc-t

  release:
    namespace: v1-t

environments:
  - environment: dev
    services:
      - repo: robokit
        owner: scalecube
        cluster: nebula
