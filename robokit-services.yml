registry:
  helm: nebula
  docker: scalecube

namespaces:
  protected:
    - master
    - develop

environments:
  - environment: dev
    services:
      - repo: robokit
        owner: scalecube
        cluster: nebula
